<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="www.blackeyes.com.br.be">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Interface.SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Interface.Apresentacao"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".Interface.MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".Interface.Cadastro"
            android:label="Cadastro" />
        <activity
            android:name=".Interface.Dispositivos"
            android:label="Dispositivos" />
        <activity
            android:name=".Interface.tutoriais.Tutorial"
            android:label="Tutorial" />
        <activity android:name=".Interface.Acao" />
        <activity android:name=".Interface.tutoriais.TutoCad"
            android:label="Como cadastrar">

        </activity>
    </application>

</manifest>
